{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "pretest": "lerna exec yarn prepare",
    "test": "lerna exec yarn test"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.1.4"
  }
}
